version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: interno-prosecretaria-app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
      - ./storage/app:/var/www/html/storage/app
    networks:
      - interno-network
    depends_on:
      - mysql
      - redis
    env_file:
      - .env

  nginx:
    image: nginx:alpine
    container_name: interno-prosecretaria-nginx
    restart: unless-stopped
    ports:
      - "85:80"
    volumes:
      - ./:/var/www/html
      - ./docker/nginx.conf:/etc/nginx/nginx.conf
    networks:
      - interno-network
    depends_on:
      - app

  mysql:
    image: mysql:8.0
    container_name: interno-prosecretaria-mysql
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - interno-network
    ports:
      - "3306:3306"

  redis:
    image: redis:7-alpine
    container_name: interno-prosecretaria-redis
    restart: unless-stopped
    networks:
      - interno-network
    ports:
      - "6379:6379"

networks:
  interno-network:
    driver: bridge

volumes:
  mysql_data:
    driver: local
